// Let's output all the elements of an array using the for statement.

const animals = ["dog", "cat", "sheep"];

// Please use the for statement to output the values of an array in order.
for(let i=0;i<3;i++){
  console.log(animals[i]);
}
